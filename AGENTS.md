# Git 提交约定

完成代码实现或修改后，若用户尚未要求 commit、push 或 PR，主动询问是否需要使用 `git-commit` skill 提交 feature 分支。feature 开发完成时，再询问是否需要同步远程 main 并创建 PR。仅当用户明确要求，或已对本轮任务给出持续授权时，才执行该 skill；该 skill 的范围、验证、分支保护、rebase 与推送安全门禁始终优先适用。
