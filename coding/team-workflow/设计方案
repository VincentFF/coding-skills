# Agent Teamwork 设计方案

## 范围

本方案定义高风险、跨模块改动场景下的多 agent 治理执行架构：任务分解、串行与并行路由、并行 lane 的隔离执行与合并交付。
Dispatch 机制（async 启动、steer/resume、evidence、isolation 参数）遵循 pi-subagents skill 的定义。

## 术语

- **编排层（parent）**：持有用户对齐、计划、路由、仲裁与最终验收的 agent 会话。
- **执行层（child worker）**：在指定 worktree 内完成单一 lane 写任务的 subagent run。
- **lane**：一个独立子任务的执行上下文，绑定一份文件认领、一个 worktree、一组验证命令。
- **lane board**：parent 在第一次 mutation 前记录的并行执行台账：lane、repo/cwd、认领文件、隔离路径、验证命令、独立理由。
- **packet**：发给 child 的自包含任务包：目标、repo/cwd/ref、编辑边界、约束、验收标准、验证命令、输出契约、stop/ask 条件。
- **wave**：一组并行 lane 的协同启动。
- **可分离性检查**：判断任务分解是否适合并行执行的三项判据。
- **merge 门**：所有 lane 完成后、交付前的合并与验证阶段。

## 总体架构

### 三层结构

架构由 1 个主 skill + 1 个披露文件 + 1 个 child-facing skill 组成：

| 组件 | 读者 | 加载方式 | 内容 |
|---|---|---|---|
| `team-workflow` | parent | model-invoked（触发词） | 主流程：recon → plan → 路由 → 两分支执行 → merge 门 → 交付 |
| `parallel-lanes.md` | parent | context pointer，仅路由到并行分支时加载 | 编排侧并行细节：lane board 模板、wave 形状、review 顺序、merge 与冲突反馈、cleanup |
| `lane-worker` | child worker | dispatch 时经 `skill` 参数按名挂载 | lane 纪律与写-验证循环契约 |

机制依据（pi-subagents 文档确认）：

- child 默认不继承父 skill，dispatch 时经 `skill` 参数显式挂载。
- `context: "fresh"` 覆盖 builtin worker 默认的 fork，保证 child 携带干净 context。
- 每个 packet 自包含，不依赖父对话历史。

### 按读者切分内容

skill 对读者即指令，内容按其读者切分：

- review 循环、路由、merge、仲裁只能由 parent 执行：child 无 `subagent` 工具，且 fresh-context 对抗评审按定义是另一个 agent。
- child 读到编排指令会产生执行风险：contact_supervisor 噪音、自审表演。
- 因此并行流程拆为 parent 读 `parallel-lanes.md`、child 读 `lane-worker`，两者无重叠内容。

## 路由设计

### 进入并行分支的条件

以下两个条件同时满足才进入并行分支：

1. 用户明确要求并行执行。
2. 任务分解通过可分离性检查。

任一条件不满足 → 串行分支（单 writer），并向用户说明原因。

### 可分离性检查

任务先按 source seam / 决策边界分解，再对每个子任务检查：

1. **文件认领不相交**：包括 lockfile、生成代码、barrel/index 重导出、共享配置、测试快照等隐藏共享点。
2. **无语义耦合**：lane 之间不调用、不依赖彼此将要修改的接口。
3. **独立决策与产出**：每个 lane 有自己的决策和可验证的产出，不是同一工作的机械切分。

## 并行执行设计

### Lane board

- 第一次 mutation 前，parent 记录 lane board。
- lane board 随计划一并提交用户批准。

### Wave 启动

- 一个 async `workflowScript` + `runs.all` 发起整个 wave。
- 每个 run 的配置：`agent: worker`、`context: "fresh"`、`worktree: true`、自包含 packet、`skill: ["lane-worker"]`。
- worktree 由 parent 在 launch 时分配；worker 不自建 worktree、不执行 git worktree 命令、不 commit。

### 每 lane review 循环

- reviewer 在 fresh context 中检查该 lane 的 worktree 与 diff。
- 存在 actionable findings：resume 同一 worker run 修复并重新验证，再 resume 同一 reviewer 确认 findings 已被处置。
- 同一争议经两轮仍未通过：停止该 lane 的循环，parent 带双方立场报用户仲裁。

### Merge 门

按序执行：

1. 每个 lane 先通过自己的 review 门（pre-merge）。
2. parent 串行 merge 各 lane 回主 worktree。
3. merge 后运行一次全量验证。各 lane 独立验证通过不代表合并后通过——语义冲突（文本 merge 干净但 build 失败）只有这一步能发现。
4. 验证通过后由 parent 统一 commit。
5. handoff 确认 durable 后 cleanup 各 lane worktree。

## 通用规则

- 分解的第一轴是 seam / 决策边界；并行性是分解完成后检查的属性，不作为分解依据（不 manufacture parallelism）。
- 单一 writer per worktree；所有写操作经过 lane 对应的 worker run。
- review 循环、仲裁、merge、commit 永远在 parent。
- lane 基础设施失败（launch/tooling/prompt runtime）：停止该 lane，保留 worktree 与 artifact 现场，所有权不确定前不启动替代 writer。
- 稳定流程放 skill、可变任务数据放 packet：全部 lane worker 挂载同一 `lane-worker`，保证纪律逐字一致。

## 附录 A：决策记录

| 决策 | 结论 | 理由 |
|---|---|---|
| 初稿：3 个 skill（teamwork / workflow / parrawork）互相调用 | 否决 | pi 中 skill 是上下文注入，无调用/返回语义；"仅可被 teamwork 调用"无机制支撑（model-invoked 任何来源都可触发，user-invoked 则 skill 间不可达）；teamwork 与 workflow 的 description 会争夺同一组触发词 |
| 初稿：每个 worktree 进入 parrawork 流程 | 修正 | 原意是 child 携带 packet + skill 进入 worktree，该模型成立（`skill` 参数 + fresh context）；但 parrawork 含 review 循环，child 无法执行，故拆为 parent 披露文件 + child-facing skill |
| worker 自建独立 worktree | 否决 | worktree 分配是编排动作，须在第一次 mutation 前记录于 lane board；worker 自建导致所有权无记录，cleanup 与故障恢复失效 |
| 按是否能并行来分配子任务 | 否决 | 按并行性驱动分解即 manufacture parallelism；分解按 seam，并行性事后检查 |
| child-facing skill 命名 parrawork | 修正 | 命名为 `lane-worker`，名称锚定读者与使用场景 |

## 附录 B：待决事项

- `lane-worker` 的内容厚度未确定。判定标准：packet 一旦开始重复陈述稳定纪律（报告格式、验证礼仪、升级协议），该内容即移入 skill。
- pi-subagents 文档未说明 `skill` 参数对 `disable-model-invocation: true` 的 skill 的加载行为；`lane-worker` 落地前需实测一次挂载。
